import { JSX } from "react";

export type testimonialsTypes = {
    message: string;
    img: string;
    name: string;
    designation: string;
  };

  export type ourTeamTypes = {
     img: string;
    name: string;
    designation: string;
    icons:JSX.Element[];
  };
